<div class="page-header">
<h1>Manage clients <small></small></h1>
</div>

  <div class="control-group">
    <!-- <a class="action pull-right" title="Add child"><i class="icon-plus"></i></a> -->
    <div class="btn-group pull-right">
      <%= link_to new_settlor_path do %>
      <button class="btn"><i class="icon-plus"></i> Add client</button>
      <% end %>
      <!-- <button class="btn">2</button> -->
      <!-- <button class="btn">3</button> -->
    </div>
  </div>

  <div style="clear: both; height: 1em; "></div>

<table class="table table-striped table-bordered">
  <tr>
    <th>Full legal name</th>
    <th>Born at</th>
    <th>Us citizen</th>
    <th>Marital status</th>
    <th></th>
  </tr>

  <% @settlors.each do |settlor| %>
    <%= render partial: 'form_settlor', object: settlor, as: :settlor %>
  <% end %>

</table>


<%#= link_to 'New Settlor', new_settlor_path %>
