<%= form_for(@simple_value) do |f| %>
  <% if @simple_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@simple_value.errors.count, "error") %> prohibited this simple_value from being saved:</h2>

      <ul>
      <% @simple_value.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
