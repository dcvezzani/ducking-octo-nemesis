<%= form_for(@dependent_relationship) do |f| %>
  <% if @dependent_relationship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dependent_relationship.errors.count, "error") %> prohibited this dependent_relationship from being saved:</h2>

      <ul>
      <% @dependent_relationship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :parent_id %><br />
    <%= f.number_field :parent_id %>
  </div>
  <div class="field">
    <%= f.label :dependent_id %><br />
    <%= f.number_field :dependent_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
