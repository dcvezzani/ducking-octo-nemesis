<%= form_for(@question_category) do |f| %>
  <% if @question_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question_category.errors.count, "error") %> prohibited this question_category from being saved:</h2>

      <ul>
      <% @question_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
